
// Define TypeScript interface for service `transaction`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// tslint:disable-next-line:no-empty-interface
export interface TransactionBase {
  // !code: interface // !end
}

// tslint:disable-next-line:no-empty-interface
export interface Transaction extends TransactionBase {
  // !code: more // !end
}

// !code: funcs // !end
// !code: end // !end
