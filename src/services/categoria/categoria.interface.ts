
// Define TypeScript interface for service `categoria`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// tslint:disable-next-line:no-empty-interface
export interface CategoriaBase {
  // !<DEFAULT> code: interface
  id: unknown;
  name: string;
  // !end
}

// tslint:disable-next-line:no-empty-interface
export interface Categoria extends CategoriaBase {
  // !<DEFAULT> code: more
  id: any; // change if needed
  // !end
}

// !code: funcs // !end
// !code: end // !end
